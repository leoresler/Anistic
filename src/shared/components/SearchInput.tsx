export function SearchInput() {
  return (
    <div className="input-surface">
      <span className="text-base text-[--color-gold]">⌕</span>
      <span>Search title, studio, mood, or atmosphere...</span>
    </div>
  );
}
